# Server-side database connection (used by API routes)
# Get this from Supabase Dashboard > Project Settings > Database > Connection string
# IMPORTANT: do NOT prefix with NEXT_PUBLIC_* (keeps it server-side only)
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.YOUR_PROJECT.supabase.co:5432/postgres

# Admin Configuration
# This should be a secure, unique password (at least 16 characters)
# Only you should know this - it grants access to all user conversations
ADMIN_PASSWORD=change-this-to-something-secure

# Push Notifications (optional)
# Generate VAPID keys by running: node scripts/generate-vapid-keys.js
# NEXT_PUBLIC_VAPID_PUBLIC_KEY is safe to expose (used by browsers to subscribe)
# VAPID_PRIVATE_KEY must remain secret (used to send push notifications)
NEXT_PUBLIC_VAPID_PUBLIC_KEY=your-vapid-public-key
VAPID_PRIVATE_KEY=your-vapid-private-key
